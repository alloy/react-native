'use strict';












const React = require('react');

/**
 * Whether the current element is the descendant of a <Text> element.
 */
module.exports = (React.createContext(false) as React$Context<$FlowFixMe>);
