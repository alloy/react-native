'use strict';;
import Platform from '../Utilities/Platform';
import normalizeColor from './normalizeColor';

/* eslint no-bitwise: 0 */
function processColor(color?: (string | number) | null | undefined): number | null | undefined {
  return null as any;
}

export default processColor;;
